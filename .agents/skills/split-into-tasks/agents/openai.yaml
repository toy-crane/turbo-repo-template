interface:
  display_name: "Split Into Tasks"
  short_description: "Split specs into deliverable tasks and review gates"
  default_prompt: "Use $split-into-tasks to turn this spec into the fewest independently deliverable tasks with only risk-justified review checkpoints."
