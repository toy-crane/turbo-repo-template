interface:
  display_name: "Implement"
  short_description: "Implement a spec through review and runnable handoff"
  default_prompt: "Use $implement to implement this spec folder, run its required verification and automated code review, and hand off a verified runnable product address when the repository provides one."
