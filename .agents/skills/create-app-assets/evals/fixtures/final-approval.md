# Final approval state

The user selected the shared shopping-basket mark, reviewed the separated
background, foreground, and monochrome layers, and confirmed that their
recomposition matches the chosen preview at full size and 60×60. The user now
explicitly approves those attached layers for active app icon and splash use.

Approved palette: warm coral `#F2644B`, cream `#FFF8E8`, warm sage `#74B88F`.
No text, rounded mask, shadow, blur, glass effect, or store artwork is approved.

Final generation prompt:

> Use case: logo-brand. Create a square app-icon concept for a family shared
> shopping-list app. Center a simple cream shopping basket with a coral plus on
> a flat green background. Keep the mark readable at 60×60. No text, device,
> UI, badge, rounded mask, shadow, blur, glass effect, or watermark.

Final editing prompt:

> Use the selected preview as the edit target. Change only the background from
> `#58BFA6` to the slightly warmer sage `#74B88F`. Preserve the basket, plus,
> proportions, placement, cream and coral colors, flat treatment, and every
> negative constraint exactly.

Existing splash backgrounds to preserve:

- Light: `#F4F4F6`
- Dark: `#0B0B0D`
