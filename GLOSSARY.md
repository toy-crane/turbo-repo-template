# 용어

## 화면에 보이는 이름

**닉네임**:
프로필 등 앱 화면에 표시되는 사용자가 정한 이름이다. 데이터 모델의 `display_name`을 화면에서는 이 이름으로만 부른다.
_피할 표현_: 이름, 표시 이름, display name

**아이디**:
계정을 구분하는 공개 고유값이다. 데이터 모델의 `username`을 화면에서는 이 이름으로만 부른다.
_피할 표현_: 사용자 이름, 사용자 아이디, 핸들

**인증 코드**:
이메일로 받는 6자리 일회용 로그인 코드다. 화면과 접근성 이름에서 이 이름만 쓴다. 코드와 관련된 짧은 문구에서는 "코드"로 줄여 쓸 수 있다.
_피할 표현_: 인증번호, 일회용 비밀번호, OTP

## UI 렌더러 경계

아래는 모바일 앱의 UI 렌더러 경계를 설명할 때 사용하는 프로젝트 표준 용어다.

**React Native UI**:
화면의 구조, 레이아웃과 상호작용을 React Native 컴포넌트가 소유하는 UI다. React Native가 최종적으로 플랫폼 네이티브 뷰를 렌더링한다는 사실만으로 플랫폼 UI라고 부르지 않는다.
_피할 표현_: RN 네이티브 UI, JavaScript UI

**네이티브 기반 RN 컴포넌트**:
React Native API와 컴포넌트 트리 안에서 사용하지만 특수한 표현이나 동작은 플랫폼 네이티브 구현이 담당하는 컴포넌트다. `GlassView`와 `BlurView`가 대표적인 예다.
_피할 표현_: 호스팅된 SwiftUI, 플랫폼 UI 컴포넌트

**호스팅된 SwiftUI**:
React Native 화면 안의 `Host` 경계를 통해 삽입한 SwiftUI 서브트리다. `Host` 바깥은 React Native가, 그 안의 렌더링과 레이아웃은 SwiftUI가 담당한다.
_피할 표현_: SwiftUI 스타일 RN 컴포넌트, 네이티브 기반 RN 컴포넌트

**호스팅된 Compose**:
React Native 화면 안의 `Host` 경계를 통해 삽입한 Jetpack Compose 서브트리다. `Host` 바깥은 React Native가, 그 안의 렌더링과 레이아웃은 Compose가 담당한다.
_피할 표현_: Compose 스타일 RN 컴포넌트, 네이티브 기반 RN 컴포넌트

**네이티브 셸**:
플랫폼 내비게이션이 소유하는 화면 외곽 구조로, 네이티브 헤더, 툴바, 모달 표시 방식과 전환 제스처를 포함한다.
_피할 표현_: 화면 콘텐츠, React Native UI 셸

**플랫폼 UI**:
React Native UI와 큰 경계를 대비할 때 호스팅된 SwiftUI, 호스팅된 Compose와 네이티브 셸을 함께 가리키는 상위 용어다. 구체적인 구현을 말할 때는 더 정확한 하위 용어를 사용한다.
_피할 표현_: 네이티브 UI
