diff --git a/android/src/main/java/expo/modules/ui/SwitchView.kt b/android/src/main/java/expo/modules/ui/SwitchView.kt
index 13da542..9f0a2e8 100644
--- a/android/src/main/java/expo/modules/ui/SwitchView.kt
+++ b/android/src/main/java/expo/modules/ui/SwitchView.kt
@@ -6,0 +7,2 @@ import androidx.compose.runtime.Composable
+import androidx.compose.ui.semantics.contentDescription
+import androidx.compose.ui.semantics.semantics
@@ -43,0 +46 @@ data class SwitchProps(
+  val accessibilityLabel: String? = null,
@@ -58 +61,5 @@ fun FunctionalComposableScope.SwitchContent(
-    modifier = ModifierRegistry.applyModifiers(props.modifiers, appContext, composableScope, globalEventDispatcher),
+    modifier = ModifierRegistry
+      .applyModifiers(props.modifiers, appContext, composableScope, globalEventDispatcher)
+      .semantics(mergeDescendants = true) {
+        props.accessibilityLabel?.let { contentDescription = it }
+      },
diff --git a/build/jetpack-compose/Switch/index.d.ts b/build/jetpack-compose/Switch/index.d.ts
index 559b1e7..65824b9 100644
--- a/build/jetpack-compose/Switch/index.d.ts
+++ b/build/jetpack-compose/Switch/index.d.ts
@@ -33,0 +34,4 @@ export interface SwitchProps {
+    /**
+     * Accessibility label announced for the switch.
+     */
+    accessibilityLabel?: string;
diff --git a/build/universal/Switch/types.d.ts b/build/universal/Switch/types.d.ts
index d3e6e4e..7098bf0 100644
--- a/build/universal/Switch/types.d.ts
+++ b/build/universal/Switch/types.d.ts
@@ -17,0 +18,4 @@ export interface SwitchProps {
+    /**
+     * Accessibility label announced for the switch without rendering visible text.
+     */
+    accessibilityLabel?: string;
diff --git a/expo-module.config.json b/expo-module.config.json
index 3cba55e..0ebfdda 100644
--- a/expo-module.config.json
+++ b/expo-module.config.json
@@ -9,7 +9 @@
-    "modules": ["expo.modules.ui.ExpoUIModule"],
-    "publication": {
-      "groupId": "expo.modules.ui",
-      "artifactId": "expo.modules.ui",
-      "version": "57.0.9",
-      "repository": "local-maven-repo"
-    }
+    "modules": ["expo.modules.ui.ExpoUIModule"]
diff --git a/src/jetpack-compose/Switch/index.tsx b/src/jetpack-compose/Switch/index.tsx
index b6268fe..b5373cc 100644
--- a/src/jetpack-compose/Switch/index.tsx
+++ b/src/jetpack-compose/Switch/index.tsx
@@ -39,0 +40,4 @@ export interface SwitchProps {
+  /**
+   * Accessibility label announced for the switch.
+   */
+  accessibilityLabel?: string;
diff --git a/src/universal/Switch/index.android.tsx b/src/universal/Switch/index.android.tsx
index c2ddf6c..9792473 100644
--- a/src/universal/Switch/index.android.tsx
+++ b/src/universal/Switch/index.android.tsx
@@ -1,2 +1,10 @@
-import { Row, Switch as ComposeSwitch, Text } from '@expo/ui/jetpack-compose';
-import { testID as testIDModifier, weight } from '@expo/ui/jetpack-compose/modifiers';
+import {
+  Row,
+  Switch as ComposeSwitch,
+  Text,
+  useMaterialColors,
+} from '@expo/ui/jetpack-compose';
+import {
+  testID as testIDModifier,
+  weight,
+} from '@expo/ui/jetpack-compose/modifiers';
@@ -6 +14,10 @@ import type { SwitchProps } from './types';
-export function Switch({ value, onValueChange, label, disabled, testID, modifiers }: SwitchProps) {
+export function Switch({
+  value,
+  onValueChange,
+  label,
+  accessibilityLabel,
+  disabled,
+  testID,
+  modifiers,
+}: SwitchProps) {
+  const colors = useMaterialColors();
@@ -8,0 +26 @@ export function Switch({ value, onValueChange, label, disabled, testID, modifier
+      accessibilityLabel={accessibilityLabel ?? label}
@@ -12 +30,4 @@ export function Switch({ value, onValueChange, label, disabled, testID, modifier
-      modifiers={[...(modifiers ?? []), ...(testID ? [testIDModifier(testID)] : [])]}
+      modifiers={[
+        ...(modifiers ?? []),
+        ...(testID ? [testIDModifier(testID)] : []),
+      ]}
@@ -20 +41,3 @@ export function Switch({ value, onValueChange, label, disabled, testID, modifier
-      <Text modifiers={[weight(1)]}>{label}</Text>
+      <Text color={colors.onSurface} modifiers={[weight(1)]}>
+        {label}
+      </Text>
diff --git a/src/universal/Switch/index.ios.tsx b/src/universal/Switch/index.ios.tsx
index 084ab71..165f99e 100644
--- a/src/universal/Switch/index.ios.tsx
+++ b/src/universal/Switch/index.ios.tsx
@@ -2 +2,4 @@ import { Toggle } from '@expo/ui/swift-ui';
-import { disabled as disabledMod } from '@expo/ui/swift-ui/modifiers';
+import {
+  accessibilityLabel as accessibilityLabelMod,
+  disabled as disabledMod,
+} from '@expo/ui/swift-ui/modifiers';
@@ -10,0 +14 @@ export function Switch({
+  accessibilityLabel,
@@ -15,0 +20 @@ export function Switch({
+  if (accessibilityLabel) modifiers.push(accessibilityLabelMod(accessibilityLabel));
diff --git a/src/universal/Switch/types.ts b/src/universal/Switch/types.ts
index 11cc2a1..4e3a4a0 100644
--- a/src/universal/Switch/types.ts
+++ b/src/universal/Switch/types.ts
@@ -21,0 +22,5 @@ export interface SwitchProps {
+  /**
+   * Accessibility label announced for the switch without rendering visible text.
+   */
+  accessibilityLabel?: string;
+
